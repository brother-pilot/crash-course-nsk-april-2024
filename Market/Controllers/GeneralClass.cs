using Market.DAL;
using Market.Misc;
using Microsoft.AspNetCore.Mvc;

namespace Market.Controllers;

internal static class GeneralClass
{
    /*public static IActionResult GetResponceFromDB<T>(DbResult dbResult,object res)
    {
        return ParserDbResult.DbResultIsSuccessful(dbResult, out var error)
            ? new JsonResult(dbResult)
            : error;
    }
    */
    
}